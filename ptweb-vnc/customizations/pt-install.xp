#!/usr/bin/expect 
set timeout 86400

# Call a small shell installer that finds the .deb and runs apt-get (avoids Expect/Tcl interpolation)
spawn /pt-install.sh

#expect "\\\[More\\\]"
#send "q\r"

expect "Do you accept the terms of this EULA? \\\[yes/no\\\]"
send "yes\r"

expect eof
